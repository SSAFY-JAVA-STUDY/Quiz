## 2주차 퀴즈

#### 1. 아래 코드의 실행결과와 그 이유를 설명하시오.(기훈)
``` java
if(x=0)
```
답변)
변수 x 에 0이 할당된 후에 추가 논리 연산을 수행하게 됩니다.

<br>

#### 2. 아래 코드의 빈칸에 들어갈 정답과 시간복잡도를 설명하시오.(기훈)
``` java
public class Sort {
 
    public static void swap(int[] arr, int idx1, int idx2){
        int temp = arr[idx1];
        arr[idx1] = arr[idx2];
        arr[( 1 )] = temp;
    }
 
    public static void Usort(int[] array, int length){
        for (int i = 0; i < length; i++) {
            for (int j = 0; j < length - i - 1; j++) {
                if (array[j] > array[j + 1]) {
                    swap(array, j, j + 1);
                }
            }
        }
    }
 
    public static void main(String[] args) {
        int[] item = new int[] { 5, 3, 8, 1, 2, 7 };
        int nx = 6;   
        Usort(item, ( 2 ));
 
        for (int data : item) {
            System.out.print(data + " ");
        }
    }
}
______________________________________________________________
출력값
1 2 3 5 7 8
```
답변) (1) idx2, (2) item.length<br>
시간복잡도는 O(N^2) 입니다. i = 0 일 때 j 가 N - 1번, i = 1일 때 N - 2번, ... i = N - 1일 때 j 가 0번 반복되는 코드이므로<br>
0 + 1 + ... + N - 2 + N - 1 을 계산해보면 O(N^2) 이 됩니다.


<br>

#### 3. 아래 코드의 정답과 그 이유를 설명하시오.(기훈)

``` java
public class GradeCalculator {
    public static void main(String[] args) {
        int[] n = {73, 95, 82}; 
        int sum = 0;


        for(int i = 0; i < 3; i++) {
            sum += n[i];
        }


        switch(sum / 30) {
            case 10:
            case 9:
                System.out.println("A");
            case 8:
                System.out.println("B");
            case 7:
            case 6:
                System.out.println("C");
            default:
                System.out.println("D");
        }
    }
}

```
답변) "B\nC\nD"<br>
sum 이 250 이므로 250 / 30 은 8 이 됩니다.<br>
따라서 case 8: 에 해당하게 되는데 이 때 break 가 없으면 flow 가 아래로 계속 흐르게 되므로
B C D 가 모두 출력됩니다.

<br>

##### 4. 다음 switch문의 결과를 예측하고 출력이 제대로 되기 위해 코드를 수정하시오. (지영)

```java

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Quiz04 {

	public static void main(String[] args) throws IOException {
		System.out.print("현재 월을 입력하세요.>");

		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int month = Integer.parseInt(br.readLine()); // 몇 월?? 4

		switch(month) {
		case 3:	case 4:	case 5:
			System.out.println("현재의 계절은 봄입니다.");

		case 6: case 7: case 8:
			System.out.println("현재의 계절은 여름입니다.");		
			break;
		case 9: case 10: case 11:
			System.out.println("현재의 계절은 가을입니다.");		
			break;
		default:
		case 12: case 1: case 2:
			System.out.println("현재의 계절은 겨울입니다.");
		}
	}
}

```
답변) "현재의 계절은 봄입니다.\n현재의 계절은 여름입니다."<br>
현재는 4월이므로 4를 입력하게 되면 case 4: 에 해당하는데 이 때 break 가 없으므로 그 아래로 flow 가 흘러가게 됩니다.<br>
따라서 여름을 출력하는 부분에서 여름을 출력하고 break 를 만나 switch-case 문을 탈출하게 되므로 위와 같은 답이 나옵니다.

<br>

##### 5. 다음 반복문을 향상된 for문을 이용하여 다시 작성하시오.(지영)

```java

public class Quiz05 {

	public static void main(String[] args)  {
		int[] arr = {1, 3, 5, 7, 9};
		for(int i = 0; i < arr.length; i++ ) {
			System.out.printf("%5d", arr[i]);			
		}
	}
}

```
정답
```
int[] arr = {1, 3, 5, 7, 9};
for (int num : arr) {
    System.out.printf("%5d", num);
}
```

<br>

##### 06. 배열의 선언 및 초기화가 잘못 된 것을 고르고 그 이유를 설명하시오.(지영)
```

1)  int[] arr[];

2)  int[] arr = {1, 2, 3, }

3)  int[] arr = new int[5];

4)  int[] arr = new int[5]{1, 2, 3, 4, 5};

5)  int[] arr[] = new int[3][];

```
답변) 4<br>
배열을 하드코딩 하게 될 경우 하드코딩하는 내용에 따라 배열의 크기가 결정되므로 [ ] 안에 사이즈를 지정할 수 없습니다.

<br>

#### 7. 아래 코드의 실행결과를 설명하시오.(민아) 

```java
public class Main {
    public static void main(String [] args){
        int i=5;
        while(i--!=0){
            System.out.println(i);
        }
    }
}
```
답변)
```
4
3
2
1
0
```
while 문에서 조건 부분에 i-- 는 i 가 0 과 같은 지 확인 후 i 의 값을 1 감소 시키는 코드입니다.<br>
따라서 조건부에 5 가 들어가면 4, 4 가 들어가면 3, 3 이 들어가면 2, 2 가 들어가면 1, 1이 들어가면 0 이 출력되게 됩니다.

<br>

#### 8. switch문의 제약 조건 두 가지를 설명하시오.(민아) 
답변)<br>
1 - switch 문의 값은 정수 또는 문자열이어야 한다.
2 - case 문의 값에는 정수 상수와 문자열만 가능하다


#### 9. String 클래스의 메서드 중 하나를 설명하시오.(민아) 
답변) valueOf()<br>
String 클래스의 static method 중 valueOf 는 int, char, float 등 다른 타입의 값을 String 타입의 객체로 변환해주는 메서드 입니다.

<br>


#### 10. 다음 do-while문을 while문으로 변환하세요.(성훈)
```
import java.util.Scanner;

public class aaa {
  public static void main(String[] args) {
      int input = 0, answer = 0;
      answer = 50;
      Scanner scanner = new Scanner(System.in);
      do {
        System.out.print (" 1과 100사이의 정수를 입력하세요.") ;
        input = scanner.nextInt();
        if(input > answer) {
          System. out. println (" 더 작은 수 로 다시 시도해보세요. " );
        } else if(input < answer) {
          System.out.println ( "더 큰 수 로 다시 시도해보세요. ") ;
        }
      } while(input!=answer);
      System.out.println (" 정답입니다. ") ;

    }
}
```
답변)
```
int input = 0, answer = 0;
answer = 50;
Scanner scanner = new Scanner(System.in);
while (true) {
    System.out.print (" 1과 100사이의 정수를 입력하세요.") ;
    input = scanner.nextInt();
    if(input > answer) {
      System. out. println (" 더 작은 수 로 다시 시도해보세요. " );
    } else if(input < answer) {
      System.out.println ( "더 큰 수 로 다시 시도해보세요. ") ;
    }
    
    if (input == answer) break;
}

System.out.println("정답입니다. ");
```

<br>

#### 11. 다음 빈칸에 설명에 맞는 String클래스 메서드를 작성하세요. (성훈)

| 메서드           |설명|
|---------------|---|
| charAt()      |문자열에서 해당 위치(index)에 있는 문자를 반환한다.|
| length()      |문자열의 길이를 반환한다.|
| substring()   |문자열에서 해당 범위 (from~to)에 있는 문자열을 반환한다.|
| toCharArray() |문자열을 문자배열 (char[])로 변환해서 반환한다.|

#### 12. 스터디원들의 이름을 넣은 2*3배열을 생성하고, 한 행씩 출력하세요.(성훈)
답변)
```
String[][] members = {
    {"기훈", "지영", "민아"},
    {"성훈", "현수", "선민"}
};

for (int i = 0; i < 2; i++) {
    for (int j = 0; j < 3; j++) {
        System.out.print(members[i][j] + " ");
    }
    System.out.println();
}
```

<br>

#### 13. 조건문과 반복문에 대해 잘못 설명한 것은 무엇입니까?(현수)
```
1)  if 문은 조건식의 결과에 따라 실행 흐름을 달리할 수 있다.

2)  switch 문에서 사용할 수 있는 변수의 타입은 int, double이 될 수 있다.

3)  for 문은 카운터 변수로 지정한 횟수만큼 반복시킬 때 사용할 수 있다.

4)  break 문은 swich 문, for 문, while 문을 종료할 때 사용할 수 있다.

```
답변) 2번<br>
switch 문에서는 정수와 문자/문자열 만 사용 가능합니다.

<br>


#### 14. 주어진 배열 항목에서 최대값을 출력하는 코드를 작성해보세요(for문 이용).(현수)
```java
int[] array = {1, 5, 3, 8, 2};
```
답변)
```
int max = array[0];
for (int i = 1; i < array.length; i++) {
    if (max < array[i]) {
        max = array[i];
    }
}
System.out.println(max);
```

<br>

#### 15. 주어진 배열 항목의 전체 합과 평균을 구해 출력하는 코드를 작성해보세요(중첩 for문 이용).(현수)
```java
int[][] array = {
    {95, 86},
    {83, 92, 96},
    {78, 83, 93, 87, 88}
};
```
답변)
```
int[][] array = {
    {95, 86},
    {83, 92, 96},
    {78, 83, 93, 87, 88}
};

int sum = 0;
int cnt = 0;
for (int i = 0; i < array.length; i++) {
    for (int j = 0; j < array[i].length; j++) {
        sum += array[i][j];
        cnt++;
    }    
}

System.out.println("총합 : " + sum);
System.out.println("평균 : " + ((double) sum / cnt));
```


<br>

##### 16. 아래 코드에서 break 를 1번만 사용해 print 문이 1번만 실행되도록 하세요. (선민)
```java
public class Main {

    public static void main(String[] args) {
        for (int a = 0; a <= 30; a++) {
            for (int b = 0; b <= 30; b++) {
                for (int c = 0; c <= 30; c++) {
                    if (a + b + c == 30) {
                        System.out.println(a + " " + b + " " + c);
                    }
                }
            }
        }    
    }
    
}
```
답변)
```
loop : for (int a = 0; a <= 30; a++) {
    for (int b = 0; b <= 30; b++) {
        for (int c = 0; c <= 30; c++) {
            if (a + b + c == 30) {
                System.out.println(a + " " + b + " " + c);
                break loop;
            }
        }
    }
}    
```

<br>

##### 17. 아래는 Merge Sort 에서 두 배열을 합하는 과정의 코드입니다. 설계 주석에 맞춰 반복문, 배열을 이용해 코드를 완성해주세요.(선민)
```java
import java.utils.Arrays;

public class Main {
    public static void main(String[] args) {

        int[] arr1 = {3, 6, 7};
        int[] arr2 = {1, 2, 4, 5};
        int[] result = new int[arr1.length + arr2.length];

        int i = 0; // arr1 을 탐색할 index
        int j = 0; // arr2 를 탐색할 index
        int k = 0; // result 에 값을 넣기 위해 사용할 index

        /*
        1. arr1[i] 와 arr2[j] 를 비교해 더 작은 값을 result 배열에 넣는다.
        2. i 또는 j 가 각 배열의 마지막 인덱스를 넘어설 때까지 1번 과정을 반복한다.
        3. i 가 arr1 의 마지막 인덱스를 넘어섰다면 j 를 끝까지 이동시키며 arr2에 남은 값을 result 에 넣고,
        그 반대라면 i 를 끝까지 이동시키며 arr1에 남은 값을 result 에 넣는다.
        */

        // 여기에 코드 작성해주세요
        
        // 정렬된 배열 출력 결과 : [1, 2, 3, 4, 5, 6, 7]
        System.out.println(Arrays.toString(result));
    }
}

```
답변)
```
int[] arr1 = {3, 6, 7};
int[] arr2 = {1, 2, 4, 5};
int[] result = new int[arr1.length + arr2.length];

int i = 0; // arr1 을 탐색할 index
int j = 0; // arr2 를 탐색할 index
int k = 0; // result 에 값을 넣기 위해 사용할 index

/*
1. arr1[i] 와 arr2[j] 를 비교해 더 작은 값을 result 배열에 넣는다.
2. i 또는 j 가 각 배열의 마지막 인덱스를 넘어설 때까지 1번 과정을 반복한다.
3. i 가 arr1 의 마지막 인덱스를 넘어섰다면 j 를 끝까지 이동시키며 arr2에 남은 값을 result 에 넣고,
그 반대라면 i 를 끝까지 이동시키며 arr1에 남은 값을 result 에 넣는다.
*/

// 여기에 코드 작성해주세요
while (i < arr1.length && j < arr2.length) {
    if (arr1[i] < arr2[j]) {
        result[k++] = arr1[i++];
    } else {
        result[k++] = arr2[j++];
    }
}

while (i < arr1.length) {
    result[k++] = arr1[i++];
}

while (j < arr2.length) {
    result[k++] = arr2[j++];
}

// 정렬된 배열 출력 결과 : [1, 2, 3, 4, 5, 6, 7]
System.out.println(Arrays.toString(result));
```

<br>

##### 18. for 문을 이용해 예시처럼 다이아몬드 출력하는 코드를 작성하세요. 입력값(N)은 홀수만 받습니다.(선민)
```
9
    *    
   ***   
  *****  
 ******* 
*********
 ******* 
  *****  
   ***   
    *    
```
답변)
```
Scanner sc = new Scanner(System.in);
int N; // N 은 홀수만 입력해주세요
while ((N = sc.nextInt()) % 2 == 0) {
    System.out.println("홀수만 입력하세요!!!");
}

StringBuilder result = new StringBuilder();
for (int row = 0; row < N / 2; row++) {
    int star = 2 * row + 1;
    int space = N - star;

    for (int j = 0; j < space / 2; j++) {
        result.append(' ');
    }
    for (int j = 0; j < star; j++) {
        result.append('*');
    }
    for (int j = 0; j < space / 2; j++) {
        result.append(' ');
    }
    result.append('\n');
}

for (int row = N / 2; row >= 0; row--) {
    int star = 2 * row + 1;
    int space = N - star;

    for (int j = 0; j < space / 2; j++) {
        result.append(' ');
    }
    for (int j = 0; j < star; j++) {
        result.append('*');
    }
    for (int j = 0; j < space / 2; j++) {
        result.append(' ');
    }
    result.append('\n');
}

System.out.println(result);
```